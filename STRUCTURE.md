# Структура проекта

## ✅ Готово к развертыванию

### Backend (server/)

```
server/
├── index.js              # Главный файл сервера
├── database.js           # Конфигурация PostgreSQL
├── package.json          # Зависимости бэкенда
├── models/
│   ├── Tournament.js     # Модель турнира
│   └── Registration.js   # Модель регистрации
└── routes/
    ├── tournaments.js    # Маршруты турниров
    ├── registrations.js  # Маршруты регистраций
    └── admin.js          # Админ маршруты
```

### Frontend (Next.js)

```
pages/
├── _app.js               # Главный компонент Next.js
├── index.js              # Главная страница
└── tournaments/
    ├── index.js          # Список турниров
    └── [id].js           # Детали турнира

components/
├── Layout.js             # Layout компонент
├── SEO.js                # SEO компонент
├── TournamentCard.js     # Карточка турнира
├── TournamentForm.js     # Форма создания турнира
└── RegistrationForm.js   # Форма регистрации

lib/
└── translations.js       # Система переводов

styles/
└── globals.css           # Глобальные стили

public/
├── manifest.json         # PWA манифест
└── robots.txt            # Robots.txt
```

## 🔧 Конфигурация

### next.config.js
- Настроена i18n с тремя языками (kz, ru, en)
- Настроены environment variables
- Настроены rewrites для sitemap

### render.yaml
- Настроены два сервиса:
  - `tournament-server` (бэкенд)
  - `tournament-client` (фронтенд)
- Настроена база данных PostgreSQL
- Настроены environment variables

## 📦 Зависимости

### Backend (server/package.json)
- express
- cors
- pg (PostgreSQL)
- dotenv

### Frontend (package.json)
- next
- react
- react-dom
- axios
- react-icons

## 🌐 API Endpoints

### Публичные
- `GET /api/tournaments` - Все турниры
- `GET /api/tournaments/:id` - Детали турнира
- `GET /api/tournaments/upcoming/list` - Предстоящие турниры
- `POST /api/registrations` - Регистрация на турнир
- `GET /api/registrations/check` - Проверка регистрации

### Админ
- `POST /api/admin/tournaments` - Создать турнир
- `GET /api/admin/tournaments` - Все турниры (админ)
- `PUT /api/admin/tournaments/:id` - Обновить турнир
- `DELETE /api/admin/tournaments/:id` - Удалить турнир
- `GET /api/admin/tournaments/history` - История турниров

## 🌍 Языки

- Казахский (kz) - по умолчанию
- Русский (ru)
- Английский (en)

## 🗄️ База данных

### Таблицы
- `tournaments` - Турниры (title_kz, title_ru, title_en, rules_kz, rules_ru, rules_en)
- `registrations` - Регистрации
- `tournament_history` - История удаленных турниров

### Автоматическое создание
Таблицы создаются автоматически при первом запуске сервера.

## 🚀 Развертывание

### На Render.com
1. Создайте PostgreSQL базу данных
2. Создайте Web Service для бэкенда (rootDir: server)
3. Создайте Web Service для фронтенда
4. Настройте Environment Variables
5. Деплой автоматический из Git

Подробные инструкции в `DEPLOYMENT_GUIDE.md`

## ✅ Проверка

- ✅ Все компоненты связаны правильно
- ✅ Все импорты правильные
- ✅ Нет ошибок линтера
- ✅ Система переводов работает
- ✅ SEO оптимизирован
- ✅ Поддержка трех языков
- ✅ Структура бэкенда правильная
- ✅ Структура фронтенда правильная
- ✅ Конфигурация Render.com готова

## 🎯 Готово к использованию!

Проект готов к развертыванию на Render.com.

